    import app from "./app";

    /*app.listen(process.env.PORT, ()=>{
        console.log(`Server on, listening to ${process.env.PORT}`);
    });*/

    app.listen(3001, ()=>{
        console.log("Server web prendido en puerto 3001");
    });